# Scripts

## init-googletest.sh

Initializes Google Test (v1.17.0) for sim-city. Run from **2110 repo root**:

```bash
./scripts/init-googletest.sh
```

Clones googletest to `sim-city/third_party/googletest-v1.17.0` (gitignored). Then run `make test` from sim-city.
